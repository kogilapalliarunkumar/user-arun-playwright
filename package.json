{
  "name": "playwrightproject",
  "version": "1.0.0",
  "scripts": {
    "test": "rimraf allure-results allure-report test-results playwright-report && playwright test && npx playwright show-report && allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.2",
    "allure-commandline": "^2.42.1",
    "allure-playwright": "^3.10.0",
    "typescript": "^6.0.3",
    "rimraf": "^5.0.0"
  }
}

{
  "name": "playwrightproject",
  "version": "1.0.0",
  "scripts": {
    
  "test": "npx playwright test",
  "allure": "allure serve allure-results",
  "test:allure": "npx playwright test && allure serve allure-results"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.2",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.1",
    "rimraf": "^5.0.0",
    "typescript": "^6.0.3"
  }
}